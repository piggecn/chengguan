# 本次更新（v1.1.2）

## 新增

- **照片支持 Ctrl+V 直接粘贴**：电脑端在微信里复制图片后，聚焦「新增巡查 / 编辑记录 / 整改销号」页按 Ctrl+V（Mac 是 ⌘V），图片自动进照片上传框，预览和数量提示同步更新，正常提交即上传。剪贴板里的图通常没有文件名，代码会补一个再传（后端遇到空文件名的会当无效图片跳过）。手机端浏览器和 APK 内嵌浏览器拿不到系统剪贴板图片，这条只对桌面端有效

## 改名

- 应用名从「社区城管巡查」改为「城管台账」：桌面顶部导航、手机端标题、登录页品牌名、浏览器标签标题、PWA 名称与描述

## 移除

- **案件功能下线**：删掉案件登记页（`/cases`）、案件编辑页（`/case/<id>/edit`）及其更新/删除接口、统计页「案件」选项卡、顶部导航和底部导航的「案件」入口，连带清掉只被案件页用到的样式。数据库 `cases` 表保留不动（老数据不丢，备份仍是整库导出），只是界面上不再有入口

---

# 本次更新（v1.1.1）

## 新增 / 优化

- **台账导出页改完条件自动刷新**：选日期、乡镇、分类、小区任意一项，预览和下载链接立刻按新条件刷新，不再需要点「刷新预览」
- **统计页支持选具体时间 + 切换时间口径**：起止日期任意选，口径三选一 —— 智能（默认，已结案按结案时间、未结案按录入时间）、按录入时间、按结案时间；结案口径下三张卡换成「本期结案 / 截至未结 / 平均办理天数」，按乡镇显示占比。案件没有结案时间，始终按登记时间统计（页面上已标注）
- **移动端底部导航加「台账」入口**：导出页原来只有电脑顶部导航能进，手机上没有入口

## 移除

- 去掉台账导出页「按乡镇分文件夹」选项（已有乡镇筛选，重复）
- 去掉首页的「导出ZIP（原图+汇总表）」按钮和导出时间段输入 —— 首页只留筛选，导出统一走「台账导出」页
- 登录页底部的「办公室制」旧文案改为显示实际覆盖的两个乡镇

## 修复

- 统计页按乡镇的办结率整数除法（2/3 显示 66% 而非 67%）改为四舍五入

---

# 本次更新（v1.0.6）

## 修复

- **手机 App 下载台账/照片彻底修复**：不再依赖系统下载管理器（国内网络环境下它会一直"排队"不出文件），改为 App 内直接下载并保存到系统「下载」目录，下载中显示进度、完成有提示；文件保留中文名（需把手机 App 更新到 1.0.2，打开会自动提示）
- **台账导出页预览照片改为原图显示**：此前预览用的是缩略图所以看着模糊，导出的 Excel 本身一直是原图；现在页面预览与导出效果一致
